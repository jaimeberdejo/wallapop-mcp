# Internal Claude Code tooling and process/audit artifacts — kept locally, not published
.agents/
.claude/
CLAUDE.md
docs/
BUGS_AND_RISKS.md
IMPROVEMENT_REPORT.md
MCP_USAGE_GUIDE.md
TEST_MATRIX_100.md
TEST_REPORT.md

# Autonomy control/scratch files
AGENT_STOP
STEER.md
NEXT_FINDINGS.md
autopilot.log
.claude/.phase-ready

# Evidence/contract + internal hook state (regenerated each run)
test-results.json
.claude/.tick-evidence.json
.claude/.phase-base
.claude/.phase-grade
.claude/.autopilot.lock
.claude/.last-changed
.claude/.evidence-reads

# Local (machine-specific) Claude settings — never commit
.claude/settings.local.json
# Installer-generated stamps (regenerated by install.sh; not project content)
.claude/.lean-stack-version
.claude/.high-stakes-default

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage/
htmlcov/

# Node
node_modules/
.next/
dist/
build/
.turbo/

# Logs / OS / editor cruft
*.log
.DS_Store
tmp/

# Env / secrets — never commit credentials
.env
.env.local
.env.*
# ...but DO track the templates teams share intentionally.
!.env.example
!.env.sample
!.env.template
*.pem
*.key
*.p12
*.pfx
*.jks
credentials*.json
id_rsa
id_ed25519
id_ecdsa
id_dsa
secrets/
*.tfstate
*.tfvars
.envrc
.netrc
