# State

## Auto status
<!-- lean:auto:begin -->
_Auto-generated by scripts/tick.sh on each roadmap tick — do not edit between these markers._

- Last ticked: Phase 7 — Publish packaging
- Updated: 2026-07-01T21:42:11Z
- Roadmap complete — every phase is ticked. Close it with scripts/close-milestone.sh (or the milestone skill).
<!-- lean:auto:end -->

## Now
Roadmap complete — all 7 phases done and ticked. pnpm/TS/ESM MCP server with `search` and
`list_categories` tools, both live and wired end-to-end. `search` surfaces upstream failures as
proper MCP tool errors (no retry/backoff); opt-in live integration suite
(`WALLAPOP_LIVE_TESTS=1 pnpm test`) passes against the real API. Packaged for npm publish:
`bin` entry, MIT `LICENSE`, `README.md` (with unofficial/unaffiliated disclaimer), `npm pack
--dry-run` verified. Phases 2 and 6 were reclassified `supervised` -> `loopable` by explicit user
decision (ADR-002, ADR-003).

## Next action
Milestone complete. Options: run the `milestone` skill to archive this roadmap and start the
next batch, or just use the server as-is (not yet published to npm — that's a deliberate,
unautomated step).

## Open questions
- `scripts/test-evidence.sh`'s test-command resolver picks `pytest -q` over pnpm/vitest when a
  `tests/` dir exists and pytest happens to be on PATH (Python-first heuristic, false positive
  for this TS project). Worked around this session via `LEAN_TEST_CMD="pnpm test"`; future
  `/wrap`/`/autopilot` runs on this repo need the same override or a resolver fix.
- ~~Live `api/v3/search` contract unconfirmed~~ RESOLVED in Phase 4: the missing pieces were
  `filters_source`/`source` query params and `Origin`/`Referer` headers, now in
  `src/search/request.ts` and verified against a real 200 response.
- `Listing.condition` is modeled optional (`string | undefined`) — no raw item observed live
  (two categories tested: electronics, clothing) has a `condition`/`item_condition`/similar field.
  It may simply not exist on this endpoint; Phase 5/6 should watch for it across more categories
  before assuming it's truly always absent.
