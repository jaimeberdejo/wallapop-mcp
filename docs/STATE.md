# State

## Auto status
<!-- lean:auto:begin -->
_Auto-generated by scripts/tick.sh on each roadmap tick — do not edit between these markers._

- Last ticked: Phase 4 — `search` tool: response normalization
- Updated: 2026-07-01T21:21:07Z
- Next phase: Phase 5 — `search` tool: pagination & result clamping
- Next action: Auto-pagination loop driven by `max_results` (default 40, cap 200), passing the opaque `next_page` cursor through untouched (never decoded/reconstructed)
<!-- lean:auto:end -->

## Now
Phases 1-4 done and ticked. pnpm/TS/ESM project builds, tests, typechecks, lints clean.
`list_categories` tool live (997-node static tree, 18 top-level on no-query). `search` request
building + response normalization (`src/search/request.ts`, `src/search/listing.ts`) implemented
and unit-tested, and now CONFIRMED LIVE: `buildSearchRequest` gets a real 200 from
`api/v3/search`, `normalizeItem` produces the exact expected `Listing` shape from real data.
Not yet wired into an MCP tool — that's Phase 5. Phase 2 was reclassified `supervised` ->
`loopable` by explicit user decision (ADR-002, read-only unauthenticated public GET).

## Next action
Continue `/autopilot` for Phase 5 — `search` tool: pagination & result clamping (loopable).
Remaining supervised phase: Phase 6 (live integration tests) — still needs manual `/phase`.

## Open questions
- `scripts/test-evidence.sh`'s test-command resolver picks `pytest -q` over pnpm/vitest when a
  `tests/` dir exists and pytest happens to be on PATH (Python-first heuristic, false positive
  for this TS project). Worked around this session via `LEAN_TEST_CMD="pnpm test"`; future
  `/wrap`/`/autopilot` runs on this repo need the same override or a resolver fix.
- ~~Live `api/v3/search` contract unconfirmed~~ RESOLVED in Phase 4: the missing pieces were
  `filters_source`/`source` query params and `Origin`/`Referer` headers, now in
  `src/search/request.ts` and verified against a real 200 response.
- `Listing.condition` is modeled optional (`string | undefined`) — no raw item observed live
  (two categories tested: electronics, clothing) has a `condition`/`item_condition`/similar field.
  It may simply not exist on this endpoint; Phase 5/6 should watch for it across more categories
  before assuming it's truly always absent.
