# State

## Auto status
<!-- lean:auto:begin -->
_Auto-generated by scripts/tick.sh on each roadmap tick — do not edit between these markers._

- Last ticked: Phase 3 — `search` tool: request building
- Updated: 2026-07-01T21:07:21Z
- Next phase: Phase 4 — `search` tool: response normalization
- Next action: Map raw `api/v3/search` response items to the trimmed `Listing` shape (id, title, description, price, currency, one image_url, constructed item url, location, condition, created_at)
<!-- lean:auto:end -->

## Now
Phases 1-3 done and ticked. pnpm/TS/ESM project builds, tests, typechecks, lints clean.
`list_categories` tool live (997-node static tree, 18 top-level on no-query). `search` request
building (`src/search/request.ts`) implemented and unit-tested, but NOT yet wired into an MCP
tool — that's Phase 5. Phase 2 was reclassified `supervised` -> `loopable` by explicit user
decision (ADR-002, read-only unauthenticated public GET).

## Next action
Continue `/autopilot` for Phase 4 — `search` tool: response normalization (loopable).
Remaining supervised phase: Phase 6 (live integration tests) — still needs manual `/phase`.

## Open questions
- `scripts/test-evidence.sh`'s test-command resolver picks `pytest -q` over pnpm/vitest when a
  `tests/` dir exists and pytest happens to be on PATH (Python-first heuristic, false positive
  for this TS project). Worked around this session via `LEAN_TEST_CMD="pnpm test"`; future
  `/wrap`/`/autopilot` runs on this repo need the same override or a resolver fix.
- Live `api/v3/search` contract unconfirmed: every request attempt this session (varied params,
  headers) returned a bare `400` with no message body (see docs/plans/phase-3-search-request.md).
  `src/search/request.ts`'s query param names (`category_ids`, `min_sale_price`,
  `max_sale_price`, `distance` in meters, `order_by`, `next_page`) are a best-effort mapping from
  the spec's plain-English surface, unverified against the real live endpoint. Phase 6's opt-in
  live integration tests need to confirm or correct these — likely requires capturing real
  traffic from the Wallapop web/app client rather than more guessing.
