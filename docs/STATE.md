# State

## Auto status
<!-- lean:auto:begin -->
_Auto-generated by scripts/tick.sh on each roadmap tick — do not edit between these markers._

- Last ticked: Phase 5 — `search` tool: pagination & result clamping
- Updated: 2026-07-01T21:26:53Z
- Next phase: Phase 6 — Error surfacing & opt-in live integration tests
- Next action: Upstream HTTP failures surface immediately as MCP tool errors — no retry/backoff logic anywhere in the request path
<!-- lean:auto:end -->

## Now
Phases 1-5 done and ticked. pnpm/TS/ESM project builds, tests, typechecks, lints clean. Both MCP
tools live and wired end-to-end: `list_categories` (997-node static tree) and `search`
(request building + normalization + auto-pagination). Manually verified `search` against the
real live API with `keywords: "iphone"` and no location — returns real, non-empty Listings,
satisfying the spec's own success criterion. Phase 2 was reclassified `supervised` -> `loopable`
by explicit user decision (ADR-002, read-only unauthenticated public GET).

## Next action
Phase 6 — Error surfacing & opt-in live integration tests — is `supervised` (live third-party
API calls). Run `/phase` manually, not `/autopilot`, to build it. Only Phase 7 (publish
packaging, loopable) remains after that.

## Open questions
- `scripts/test-evidence.sh`'s test-command resolver picks `pytest -q` over pnpm/vitest when a
  `tests/` dir exists and pytest happens to be on PATH (Python-first heuristic, false positive
  for this TS project). Worked around this session via `LEAN_TEST_CMD="pnpm test"`; future
  `/wrap`/`/autopilot` runs on this repo need the same override or a resolver fix.
- ~~Live `api/v3/search` contract unconfirmed~~ RESOLVED in Phase 4: the missing pieces were
  `filters_source`/`source` query params and `Origin`/`Referer` headers, now in
  `src/search/request.ts` and verified against a real 200 response.
- `Listing.condition` is modeled optional (`string | undefined`) — no raw item observed live
  (two categories tested: electronics, clothing) has a `condition`/`item_condition`/similar field.
  It may simply not exist on this endpoint; Phase 5/6 should watch for it across more categories
  before assuming it's truly always absent.
