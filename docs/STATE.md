# State

## Auto status
<!-- lean:auto:begin -->
_Auto-generated by scripts/tick.sh on each roadmap tick — do not edit between these markers._

- Last ticked: Phase 1 — Project scaffold & MCP server skeleton
- Updated: 2026-07-01T20:03:34Z
- Next phase: Phase 2 — Category tree codegen & `list_categories` tool
- Next action: One-off codegen script that fetches `api/v3/categories` and flattens it into a static generated TS module (per `docs/adr/0001-static-generated-category-tree.md`), checked into the repo
<!-- lean:auto:end -->

## Now
Phase 1 (project scaffold + MCP server skeleton) done and ticked. pnpm/TS/ESM project builds,
tests, typechecks, and lints clean; the stdio server answers `tools/list` with `[]`.

## Next action
Run `/phase` to build Phase 2 — Category tree codegen & `list_categories` tool.
Note: Phase 2 is `supervised` (live call to Wallapop's API in the codegen script) — no autopilot.

## Open questions
- `scripts/test-evidence.sh`'s test-command resolver picks `pytest -q` over pnpm/vitest when a
  `tests/` dir exists and pytest happens to be on PATH (Python-first heuristic, false positive
  for this TS project). Worked around this session via `LEAN_TEST_CMD="pnpm test"`; future
  `/wrap`/`/autopilot` runs on this repo need the same override or a resolver fix.
